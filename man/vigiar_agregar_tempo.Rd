% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series.R
\name{vigiar_agregar_tempo}
\alias{vigiar_agregar_tempo}
\title{Aggregate VIGIAR data along the time dimension}
\usage{
vigiar_agregar_tempo(
  dados,
  agregar_por = c("ano"),
  variavel = NULL,
  funcoes = list(media = function(x) mean(x, na.rm = TRUE), n = length)
)
}
\arguments{
\item{dados}{A processed VIGIAR tibble (or any data frame with
\code{ano} column).}

\item{agregar_por}{Character vector of grouping variables.
Default: \code{c("ano")}. Other options: \code{"mes"},
\code{"sigla_uf"}, \code{"cod_municipio"}, \code{"regiao"}.}

\item{variavel}{Name of the numeric column to summarise. If
\code{NULL}, auto-detects based on the data class.}

\item{funcoes}{Named list of summary functions. Default:
\code{list(media = mean, n = length)}.}
}
\value{
A tibble with grouping columns and summary columns.
}
\description{
Groups data by time variables (ano, mes) and optional spatial
variables (UF, municipio, regiao) and computes descriptive
summaries of key numeric columns.
}
